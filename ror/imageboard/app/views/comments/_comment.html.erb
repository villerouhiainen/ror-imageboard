<%= div_for comment do %>
    <p>
        <strong>Posted <%= time_ago_in_words(comment.created_at) %> ago</strong>
        <small>(<%= comment.created_at %>)</small>
        <br/>
        <%= comment.body %>
    </p>
<% end %>
